{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <div class="container">
    <h1 style="text-align: center">Modifier la catégorie</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Mise a jour'}) }}

        <div class="row mt-2">
            <div class="col-2">
                <a class="btn btn-secondary" href="{{ path('category_index') }}">Retour a la liste</a>
            </div>
            <div class="col">
                {{ include('category/_delete_form.html.twig') }}
            </div>

    </div>
{% endblock %}
