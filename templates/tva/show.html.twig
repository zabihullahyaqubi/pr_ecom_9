{% extends 'base.html.twig' %}

{% block title %}Tva{% endblock %}

{% block body %}

    <div class="container">

    <h1 style="text-align: center">Tva</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tva.id }}</td>
            </tr>
            <tr>
                <th>Taux_tva</th>
                <td>{{ tva.tauxTva }}</td>
            </tr>
        </tbody>
    </table>

        <a class="btn mb-1 btn-secondary" href="{{ path('tva_index') }}">Retour a la liste</a>

        <a class="btn mb-1 btn-primary" href="{{ path('tva_edit', {'id': tva.id}) }}">Modifier</a>

    {{ include('tva/_delete_form.html.twig') }}

    </div>
{% endblock %}
